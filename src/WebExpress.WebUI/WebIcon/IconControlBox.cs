namespace WebExpress.WebUI.WebIcon
{
    /// <summary>
    /// Represents an icon for the box control, drawn as a frame with a label tab cut into its
    /// top edge and the lines of the content it encloses.
    /// </summary>
    public class IconControlBox : Icon
    {
        /// <summary>
        /// Returns the symbolic name the active icon set resolves to a css class.
        /// </summary>
        public override string Symbol => "control-box";
    }
}
