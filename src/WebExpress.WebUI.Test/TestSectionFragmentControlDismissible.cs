using WebExpress.WebCore.WebSection;

namespace WebExpress.WebUI.Test
{
    /// <summary>
    /// Represents a test section the dummy <see cref="TestFragmentControlDismissible"/>
    /// is registered for, so the fragment manager can render it in unit tests.
    /// </summary>
    internal class TestSectionFragmentControlDismissible : ISection
    {
    }
}
